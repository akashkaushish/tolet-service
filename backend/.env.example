# Copy this file to .env for local development when running Symfony outside
# the Compose-provided environment. Never commit real secrets.

APP_ENV=dev
APP_SECRET=change-me
APP_SHARE_DIR=var/share
DEFAULT_URI=http://localhost:8080

DATABASE_URL="mysql://app:app@database:3306/tolet?serverVersion=11.8.0-MariaDB&charset=utf8mb4"
